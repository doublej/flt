{
  "name": "@flights/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "flt": "./bin/flt"
  },
  "dependencies": {
    "@flights/core": "workspace:*",
    "citty": "^0.2.1",
    "jspdf": "^4.2.0",
    "jspdf-autotable": "^5.0.7"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "scripts": {
    "build": "bun build src/index.ts --bundle --outfile dist/flt.js --target bun",
    "test": "vitest run"
  }
}
